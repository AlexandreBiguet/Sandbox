fn main() {
    // The '!' means calling a macro
    println!("Hello, world!");
}

// to compile
// % rustc main.rs
// will directly create an executable (called main)
// -> how is linking performed ?
